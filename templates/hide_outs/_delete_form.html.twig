<form method="post" action="{{ path('hide_outs_delete', {'id': hide_out.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hide_out.id) }}">
    <button class="btn">Delete</button>
</form>
